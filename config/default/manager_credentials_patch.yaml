apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        env:
        - name: VULTR_API_KEY
          valueFrom:
            secretKeyRef:
              name: manager-bootstrap-credentials
              key: vultr-api-key
